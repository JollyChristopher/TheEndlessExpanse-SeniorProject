package halemaster.ee;

import halemaster.ee.Creation.TestAllCreationEpic;
import halemaster.ee.GameState.TestAllGameStateEpic;
import halemaster.ee.NPC.TestAllNPCEpic;
import halemaster.ee.UI.TestAllUIEpic;
import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses
({
  TestJson.class,
  
  TestAllUIEpic.class,
  TestAllGameStateEpic.class,
  TestAllCreationEpic.class,
  TestAllNPCEpic.class
})

/**
 * @name TestAll
 * 
 * @version 0.0.0
 * 
 * @date Oct 10, 2013
 */
public class TestAll 
{
  /*
   * This Suite only contains Suites for subsets of tests. Each Module could
   * have its own test suite as well as interactions between those tests. Each
   * module should have exactly 1 uppermost level suite, however, where all
   * Suites for that module are stored.
   */
  public static final String TEST_WORLD = "halemaster.ee.test.world.1423634";
}
